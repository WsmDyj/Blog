/**
 * @param {number[]} nums
 * @return {number}
 */
var rob = function (nums) {
  // 偷第一个房子/：dp[i] = dp[i-1] + nums[i]
  // 偷最后一个房子/：dp[i] = dp[i-2]

};

function robRange(nums, start, end) {
  
}

console.log(rob([2,3,2]));