// 分治 + 最优子结构

// 找重复性和重复子问题

```
function DP() {
  dp = [][] # 二维情况
  for (let i = 0; i<= length; i++) {
    dp[i][j] = dp[i'][j']
  }
  retun dp[m][n]
}
```

动态规划五部曲：
1、确定dp数组以及下标的含义
2、确定递推公式
3、dp数组如何初始化
4、确定遍历顺序
5、举例推导dp数组